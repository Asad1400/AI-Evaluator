# Core dependencies
fastapi>=0.109.0
uvicorn[standard]>=0.27.0
pydantic>=2.5.0
python-dotenv>=1.0.0

# NLP and ML dependencies
# Note: Install torch separately if needed: pip install torch --index-url https://download.pytorch.org/whl/cpu
sentence-transformers
transformers
accelerate
scipy
scikit-learn
numpy
